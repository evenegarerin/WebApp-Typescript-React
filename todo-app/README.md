To run the app run these three commands:

pnpm install
pnpm exec drizzle-kit push
pnpm dev

and then navigate to localhost:3000 in your browser of choice.

if you dont want to start to an empty app rename data.db.example to data.db

then, there are two users right now:

bob
bob@mail.com
bobspassword

and:

alice
alice@mail.com
alicespassword
